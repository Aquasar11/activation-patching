# Ignore data/ contents (not the dir itself, so the re-includes below apply)...
data/*
# ...but keep the two tiny demo images used by the notebook and e2e tests.
!data/red_apple.jpeg
!data/cat.jpeg
.venv/
venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage